---
title: Installation
description: Install osra and connect a page to a worker, with one expose() call on each side.
---

The package is `osra`, it's a dependency free package so you don't have to worry about bloat.

As with any other npm package, equip yourself of your favorite package manager and install it.

For our documentation, we will simply use npm as the package manager.

## Install

```sh
npm install osra
```
